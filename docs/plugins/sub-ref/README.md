# Sub-ref plugin

Tiny example role used by this site. `{sub-ref}` returns the current date when
invoked as `{sub-ref}`today`` and otherwise just echoes its argument. This keeps
simple dynamic substitutions localized to one place.
