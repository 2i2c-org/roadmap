# GitHub issue link plugin

Adds lightweight context to bare GitHub issue URLs. During the build it fetches
basic metadata (title + state) for each unique issue link in a document and
saves it as semantic classes / `title` attributes so the site can style them.
